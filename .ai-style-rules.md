# AI Style Rules: agent-plan-and-track

Commit fingerprint: `32d2933` (2026-08-24)
Scale tier: **Medium** (150 tracked files, 1 binary (`theme-showcase.pdf`); of the 149 non-binary, 10 are vendored theme data files, still not hand-authored but living under `skills/frontend-design/themes/`; the count grew by 2 this round from the new `hooks/fixtures/core-rules-digest/cases.json` and `hooks/scripts/run-core-rules-digest-fixtures.js`; the ~139-file hand-authored surface, markdown, JS, shell, PowerShell, YAML, still falls in the Medium band, and every incremental round has stayed a full close-read rather than sampling, since Medium's minimum bar (infra full-read, business layer sampled) is a floor, not a ceiling).

This repo is meta-architecture only (a set of skills/hooks/rules for AI coding agents), not application code, so the four dimensions below map to markdown skill structure, Node hook scripts, and bash utility scripts rather than a conventional app layer.

No strong-signal conflicts were found across the four scan dimensions: the codebase is unusually uniform (one disciplined author/style throughout). This file records the majority convention as binding; there was nothing to arbitrate.

## Golden Files

- [`hooks/gateguard.js`](hooks/gateguard.js): the canonical Node hook script. Demonstrates: JSDoc header block explaining purpose/wire-dialects/config env vars, `'use strict'`, alphabetical core-only `require`s, `// --- Section ---` banner comments grouping helpers, dialect-sniffing (`detectDialect`) instead of separate per-harness scripts, every fallible op wrapped in a narrow try/catch that either assigns/returns a self-evidently safe default with no comment needed (`readStdin()`'s `catch { return ''; }`, and several similar bare catches throughout) or, when the fallback changes control flow non-obviously, carries a one-line comment justifying it (see Naming & State-Control Rules below), `main()` defined last and invoked only inside a top-level `try { main() } catch { …fail open… }`. Its default posture is now mode-dependent rather than uniformly denying: warn (non-blocking, fact demand injected as `additionalContext`) on Claude and Codex, deny (blocking) on Copilot, since Copilot's `PreToolUse` contract has no soft-warn channel (`GATEGUARD_DENY`/`GATEGUARD_WARN` override either way, deny winning if both are set). The file still marks a gated path checked the moment the hook fires, in either mode, preserving loop-freedom; only the message text and whether the call blocks depend on `mode`. The two measurements backing this default, a 12-of-1,282-firings spot check and a 4-run-per-arm blocking-vs-off A/B, are documented by scope in [`skills/gateguard/SKILL.md`](skills/gateguard/SKILL.md:106-128); the hook's own inline comment only summarizes the A/B's finding (no accuracy gain, ~20% more turns), rather than restating either measurement's scope itself. Consistent with the ground-claims-in-source rule: state where a measurement is actually documented, not the strongest reading of it.
- [`hooks/delivery-gate.js`](hooks/delivery-gate.js), [`hooks/claude/suggest-compact.js`](hooks/claude/suggest-compact.js), and [`hooks/git-guard.js`](hooks/git-guard.js): same shape as gateguard.js; confirms the pattern is a repo-wide convention, not a one-off. Note the byte-for-byte identical `readStdin()` helper duplicated in all three, deliberately (see DONTs); `intEnv(name, fallback)` is duplicated between `delivery-gate.js` and `suggest-compact.js` only, since `git-guard.js` has no env-tunable int threshold (its `RACE_MS` is a hardcoded const, not `intEnv`-sourced). `git-guard.js` gives the standing protect-the-working-tree rule a mechanical backing the same way `plan-gate.js`'s mutation gate backs plan-before-you-mutate: it inspects `Bash`/`powershell` command text for a closed, named set of destructive git forms (`reset --hard`, `clean -f`, a forced push, or a worktree-discarding checkout/switch/restore) and denies the first occurrence of each kind (tracked per session-and-kind marker, so distinct kinds each get their own first-occurrence deny, not one shared deny for the whole session), quoting the rule, with a retry of that same kind passing (the same mark-at-deny-time, racing-loser-aware marker logic already recorded for `plan-gate.js`'s migration-state and attribution guards, reused here via a `claimMarker` with the identical `RACE_MS` threshold). Its `splitShellSegments` tokenizer is a third byte-for-byte copy of the one already duplicated between `hooks/claude/plan-gate.js` and `hooks/codex/plan-gate-pilot.js` (see DONTs), and its own header states the copy-exactly contract in those words. `delivery-gate.js` also carries the Stop hook's actual content-scanning logic: a `RATIONALIZATION` phrase list nudging toward verify-before-done when recent assistant text reads like an unverified claim, joined by a second, narrower check for evidence-free capitulation (an agreement opener like "you're right" paired with self-blame narration like "I over-corrected" in the same recent-text window). Both scan the same bounded recent-assistant-text tail (`recentAssistantText`) plus the hook's own `last_assistant_message` input field; the capitulation check additionally runs its match text through a `stripSecondhand()` carve-out (fenced blocks, inline code, double-quoted spans) before testing, the same quoted-text exemption the PR-body lint's voice checks already make, since assistant text routinely quotes words it didn't write. Both stay warn-only `systemMessage`s by default, consistent with this file's non-mutation-gate default, though an opt-in `DELIVERY_GATE_BLOCK=1` (see Naming & State-Control Rules) forces a blocking `decision` instead, self-limited to that one message rather than a repeat-until-stamped gate. The file's third content-scanning check, `voiceViolation()`, backs the writing-voice standing rule directly: it flags the model's own final message (`last_assistant_message`, falling back to the transcript tail's most recent assistant text block rather than the multi-message rationalization/capitulation window, since "opens with X" is a single-message property) for an em dash, an emoji (`\p{Extended_Pictographic}`, excluding ©/®/™), or a "Let me"/"I'll" opening, after the same `stripSecondhand()` carve-out the capitulation check uses. `stripSecondhand()` itself was generalized in the same round to match a fence or inline-code span against its own opening delimiter via backreference (so a `~~~` fence or a double-backtick span is stripped too), rather than hardcoding triple/single backticks.
- [`install.sh`](install.sh): the canonical bash script (the maintenance utility scripts were ported to Node for Windows support, leaving this as the one bash exemplar). Demonstrates: `#!/usr/bin/env bash`, a header comment block explaining usage and idempotency, `set -euo pipefail`, an early dependency gate (`need_jq()`) for the one nonstandard CLI it needs (POSIX core utilities like `awk`/`sed`/`cat`/`cmp` aren't gated; every `command -v` probe pairs with `>/dev/null 2>&1` so a successful check doesn't leak the executable's path onto stdout), `${VAR:-default}` env-tunable behavior (`PT_KEEP_MODEL`), `mktemp` scratch files written back through the symlink-safe `write_back`, and JSON assembled/merged via `jq` rather than string concatenation. Its Windows sibling `install.ps1` mirrors the same managed surface (parity note in both headers). `need_jq()` no longer just gates and exits: when `jq` is missing it now offers to install it, probing `jq_install_cmd()`'s closed, priority-ordered candidate table (`brew` first and never sudo-prefixed since it's user-scoped and refuses to run as root, so it's skipped entirely for root; every other manager sudo-prefixed unless already root) and, when a candidate is found, either running it straight away under `PT_INSTALL_JQ=1` (the non-interactive opt-in), prompting for `y`/`N` confirmation on an interactive TTY (`[ -t 0 ]`, prompt written to stderr so it survives stdout capture), or erroring with the exact command named in the message when neither applies. The same string both the prompt displays and `eval` runs, so the offer can never show one command and execute another; the whole gate still runs before `install_global_gitignore` or any target starts writing files, so a decline or an unresolvable probe leaves the machine untouched. This is the first script in the repo to actually execute a system-mutating command rather than only ever gating or erroring on a missing dependency.
- [`skills/plan-and-track/SKILL.md`](skills/plan-and-track/SKILL.md): the canonical skill file. Demonstrates: YAML frontmatter with `name` + a `description` that front-loads the trigger clause ("Use BEFORE...", "Use when..."), an H1 matching the skill name, then imperative numbered/bulleted H2 sections in the order the agent will actually execute them.
- [`agents/architect-reviewer.md`](agents/architect-reviewer.md) (siblings: [`debugger.md`](agents/debugger.md), [`mechanic.md`](agents/mechanic.md), [`researcher.md`](agents/researcher.md), [`security-auditor.md`](agents/security-auditor.md)): the canonical subagent-definition file. Fixed-order YAML frontmatter (`name` → `description` → `model` → `effort` → `tools`), then a body of: a one-sentence role statement, the verbatim "Your final message IS the deliverable ... Return findings, not pleasantries." boilerplate, a `## How to work:` list of bold-lead principles, and a closing "Structure the report tightly ... No filler." paragraph fixing the report order. `description` front-loads a trigger clause like a SKILL.md but adds an explicit negative case ("not for routine ...", "NOT for anything requiring ..."). `tools` is a bare comma-separated subset that encodes the agent's authority: pure reviewers get `Read, Grep, Glob`; `debugger` adds `Bash` but is prose-restricted to read-only use; only `mechanic` carries `Edit, Write`. See Naming & State-Control Rules for the model/effort pinning.
- [`hooks/claude/pretooluse-gateguard.json`](hooks/claude/pretooluse-gateguard.json) and [`hooks/copilot/pretooluse-gateguard.json`](hooks/copilot/pretooluse-gateguard.json): same logical hook, two wire dialects, side by side. The Claude/Codex shape (`PascalCase` event key, `matcher` + `hooks[].type/command`) and the Copilot shape (`version:1`, lowercase event key, `bash` instead of `command`, `timeoutSec` instead of `timeout`) are **not** a style choice: each is dictated by that harness's own hook contract. Match whichever dialect the target harness requires; never "normalize" one to look like the other.
- [`.github/instructions/scripts.instructions.md`](.github/instructions/scripts.instructions.md) (siblings: [`docs.instructions.md`](.github/instructions/docs.instructions.md), [`general.instructions.md`](.github/instructions/general.instructions.md)): the canonical Copilot path-scoped review-instruction file. Frontmatter is `applyTo: "<glob or comma-separated globs>"` plus a mandatory `excludeAgent: "cloud-agent"` (these are review directives for Copilot's PR reviewer, not instructions for an autonomous coding agent). Body: a one-line H1, a pointer back to whichever source backs the bucket (`.ai-style-rules.md`, the project's instructions file, README) instead of restating it, then H2 sections phrased as imperative "Flag ..." review directives. These three predate [`skills/copilot-review-instructions/`](skills/copilot-review-instructions/SKILL.md) itself (written by an earlier session before the marker convention existed, confirmed with the user rather than assumed from absence of the marker), so they now carry the `<!-- Generated by copilot-review-instructions; ... -->` marker and are fully skill-owned going forward (see DONTs).
- [`hooks/claude/plan-gate.js`](hooks/claude/plan-gate.js) (+ [`hooks/claude/pretooluse-plan-gate.json`](hooks/claude/pretooluse-plan-gate.json)): same canonical hook shape as gateguard.js/delivery-gate.js (JSDoc header, `'use strict'`, alphabetical core-only `require`s, duplicated `readStdin()`, `main()` defined last inside a top-level try/catch that fails open), but demonstrates a deliberate, self-documented variant of the loop-free-by-marking pattern: gateguard marks a file checked the moment it fires, whether that firing denies or only warns, so the retry always passes, while plan-gate intentionally denies every `tasks/todo.md` write until an external event (a `plan-and-track` Skill invocation elsewhere in the session) unstamps it, stating this explicitly in its own header ("UNLIKE GATEGUARD, REPEATED DENIAL IS INTENTIONAL"). See the hook-denial rule below. Since the last scan it also grew two more sub-gates in the same file rather than splitting into separate scripts (a hook script is scoped to what it enforces, not to one rule per file): a **scope gate** counting distinct edited files toward `PLANGATE_SCOPE_THRESHOLD` via sha256-hashed, lowercased-path marker files (Windows path-casing safety, same rationale as the existing case-insensitive `tasks/todo.md` match) guarded by a `withSessionLock()` exclusive-lockfile critical section (documented TOCTOU rationale, bounded `Atomics.wait` spin, stale-lock reclaim after 5s, fails open on any lock error); and a **content lint** that simulates an Edit/Write/MultiEdit's result in memory (never touching disk) to check new `## Plan` steps for an owner tag before allowing the write. Both new pieces keep the file's existing conventions (fail-open catches, one-line rationale comments on every non-obvious branch, config surfaced as `SCREAMING_SNAKE_CASE` env vars documented in the header) rather than introducing new ones. It has since grown a third sub-gate the same way: a **mutation gate** on a separate `Bash` PreToolUse entry, denying an outward `git push`/`gh pr create`/`gh pr merge` once a session's distinct mutation-kind count reaches `PLANGATE_MUTATION_THRESHOLD` without a stamp, classified by a quote-aware, deliberately conservative `splitShellSegments`/`detectOutwardMutations` pair (heredocs and comments stripped in unquoted context only; command substitutions and aliases are accepted false negatives, not chased). Same deny-until-stamped model as the scope gate and content lint, same header-documented config convention; [`hooks/codex/plan-gate-pilot.js`](hooks/codex/plan-gate-pilot.js) grew the identical mutation gate (its own Bash PreToolUse entry, blocking rather than the file's other warn-only checks) with the classifier duplicated verbatim rather than shared, another instance of the duplicated-helpers DONT below. It has since grown a fourth sub-gate, an **attribution guard**, back to the gateguard-style deny-once/mark-at-deny-time variant already recorded above for the migration-state guard rather than the three deny-until-stamped gates: it fires once per session when a new or changed unchecked `## Plan` step's `(main: <reason>)` tag reads as a claim about what the user did or asked rather than a fact about the work. It inspects steps that are new *or whose text changed*, via a sibling collector alongside the existing new-steps-only one, since a wrapped step carries its owner tag on a continuation line that a first-line-only newness test would miss; the older tag lint keeps inspecting only new steps, unchanged. Unlike the migration-state guard, `PLANGATE_LINT_DISABLED=1` does cover this one, since it's a phrasing tripwire on one common fingerprint rather than a data-loss guard. `hooks/codex/plan-gate-pilot.js` has since grown the identical attribution guard too, on its own `apply_patch` PreToolUse path, and its version has to work harder for the same result: rather than diffing an in-memory Edit/Write/MultiEdit simulation the way the Claude side does, it first parses the tool call's `apply_patch` command against a narrow, exact grammar for single-file structured Update-File hunks (`parseStructuredPatch`), then replays only an exact, unambiguous line match (`exactMatch`/`simulateTodoPatch`) to reconstruct the post-patch `tasks/todo.md`; any multi-file patch, non-todo target, ambiguous context, or unsupported patch syntax fails open rather than approximating. It reuses the existing session-scope state and lock rather than adding a new one, marking `mainAttributionAt` at deny time so a same-session retry within 2 seconds (a concurrent duplicate call, not an intentional retry) still denies, while a later retry passes, the same mark-at-deny-time shape as the Claude side's guard. `hooks/codex/plan-gate-pilot.js` has since grown a fifth capability on the mutation gate: a `SessionStart` hook (a fourth wiring entry, matcher `startup|resume`) that records sha256 hashes of the unchecked plan items already on disk into the session-scope record at startup or resume, so a wrapper that hides the native `apply_patch` PreToolUse/PostToolUse lifecycle (Code Mode wrapping the call inside `exec`) still lets a genuinely new unchecked plan item stamp the session once the mutation threshold is reached, validated through the same item identity, `verify:`, and owner-tag checks the native path already used. The baseline is set once per scope (a second `SessionStart` firing is a no-op if `planBaseline` already exists) and is exempted from the age-based prune, so a long-lived session doesn't lose it mid-session the way an ordinary stale scope file would be pruned. The correlation helper split in the same commit (`scopeCorrelation`, session+cwd only, feeding the new `--session-start` phase which has no `tool_use_id` yet; `correlation`, unchanged, adds `tool_use_id` on top) is a plain refactor to key that phase's state, not a new pattern. `absolutePath` also widened in the same commit to accept a safe absolute path (previously rejected outright before any containment check ran) whose canonical parent still resolves inside cwd, reusing the existing relative-path containment logic; a functional broadening riding along with the fix, not a style change.
- [`skills/humanizer/SKILL.md`](skills/humanizer/SKILL.md) (sibling: [`read-the-damn-docs/SKILL.md`](skills/read-the-damn-docs/SKILL.md)): a second skill-anatomy shape, for judgment/reference skills rather than plan-and-track's pure checklist shape. Where plan-and-track's body is entirely numbered/bulleted H2 sections in strict execution order, these front-load reference/definitional H2 sections (humanizer's `## Patterns to catch` and `## Personality and soul`, read-the-damn-docs's `## What Counts As Docs` and `## Stop Conditions`) before a late, distinctly named procedure subsection (`## Process`, `## Required Workflow`). A short top-level framing section stating output requirements (humanizer's `## Task`) doesn't count as a second competing procedure. Pick the shape by whether the skill is mostly a checklist to execute or mostly judgment calls the agent needs reference material to make.
- [`skills/frontend-design/SKILL.md`](skills/frontend-design/SKILL.md) (sibling: [`webapp-testing/SKILL.md`](skills/webapp-testing/SKILL.md)): the canonical shape for a skill vendored from an external Apache-2.0 repo rather than written in-house. Frontmatter is followed immediately by an HTML comment (`<!-- Adapted from the Apache-2.0 licensed original at <url>. Modified: <specific list of what changed>. Full license text: LICENSE.txt in this directory. -->`) naming the source, the exact edits made (never a vague "lightly edited"), and pointing at a sibling `LICENSE.txt` carrying the upstream license verbatim. `theme-factory`, formerly a third sibling in this same vendored-Apache-2.0 category, was retired this round and folded into `frontend-design` as a new `### Theming an existing artifact` subsection; its `themes/` data files and `theme-showcase.pdf` moved across unchanged, and the attribution comment grew a second sentence naming the fold-in source ("The Theming an existing artifact subsection is condensed from the same repo's theme-factory skill..."), rather than either skill keeping a separate attribution block. A future fold-in of one vendored skill into another should extend the surviving skill's attribution comment the same way, not drop the provenance of the retired one.
- [`install-office-skills.sh`](install-office-skills.sh) (sibling: [`install-office-skills.ps1`](install-office-skills.ps1)): a third bash/PowerShell installer pair alongside `install.sh`/`install.ps1`, same canonical bash shape as that Golden File (header comment block explaining purpose and why it's a separate script, `set -euo pipefail`, `command -v <tool> >/dev/null 2>&1 || { echo "error: ..." >&2; exit 1; }` dependency gates), plus a positional verb argument (`[install|uninstall]`, defaulting to install so the zero-arg invocation the docs and CI already use keeps working, validated against a closed set the way `install.sh` validates its target) and a pinned third-party CLI version (`SKILLS_CLI="skills@1.5.19"`) with its own minimum-version check, rather than an unpinned `npx <tool>@latest`. Pin external tool versions the same way when a script shells out to one.
- [`skills/migration-discipline/SKILL.md`](skills/migration-discipline/SKILL.md): the opt-in, project-scoped-skill category this repo tried (`project-skills/`, a sibling of `skills/` outside the installer's glob) was retired; the skill itself moved under `skills/` and now installs to all three harnesses through the same `skills/*/` glob as everything else, no installer change needed. Its reference material moved with it to the skill's own `references/` folder (see [`references/semantic-error-checklist.md`](skills/migration-discipline/references/semantic-error-checklist.md)), unchanged from the retired category's convention. Anatomically it's a clean instance of the reference-heavy skill shape above (`humanizer`/`read-the-damn-docs`), not a new shape: reference/definitional H2 sections (File Ownership, Porting Conventions, Worker Briefs, the Validation Ladder, etc.) precede the late named procedure subsection (`## Applying This Discipline`).
- [`skills/yeet/SKILL.md`](skills/yeet/SKILL.md): fundamentally the checklist skill shape (`plan-and-track`'s continuous step numbering carried across phase-named H2 sections: `## Publish` runs 1-6, `## Copilot review` continues 7-14), but the first skill to carry two anatomical elements no prior Golden File recorded: a pre-procedure preamble (`## Context to gather first`, an unnumbered bullet list of read-only context commands, then `## Prerequisites`, numbered 1-2 for hard-stop version/auth checks that end the skill early on failure, whose numbering the following `## Publish` section restarts from 1 rather than continues) ahead of the main numbered steps, and a closing `## Sources` section naming the exact official doc pages (the REST pulls/reviews and pulls/comments references, the GraphQL mutations reference, the GraphQL pagination guide) to revalidate against if a call starts behaving differently, since the body states live-observed endpoint/bot-login/pagination facts rather than citing them from memory. Add a `## Prerequisites` preamble to a future skill only when it has a real hard-stop dependency check to run before its steps, and a `## Sources` section only when the skill's own body asserts live-observed third-party behavior the way this one does; neither is a new baseline requirement for every skill.
- [`.github/scripts/lint-pr-body.js`](.github/scripts/lint-pr-body.js) (sibling: [`check-digest-preview.js`](.github/scripts/check-digest-preview.js)): a new script home, `.github/scripts/`, for a fourth category alongside the hook-script and skill-script shapes above: a CI guard enforcing a convention this repo's own instruction files state, rather than something an agent or hook enforces at edit/session time. Same JSDoc-header-plus-`'use strict'` opening as a hook script, and the control flow is the opposite of a hook's either way: a CI guard is meant to hard-fail loudly, never fail open. The two don't fully share a shape beyond that: `lint-pr-body.js`'s header enumerates HARD (exit 1) vs. WARN (stderr-only, exit 0) findings by number, and it's wired into its own single-purpose `.github/workflows/pr-body-lint.yml`; `check-digest-preview.js`'s header instead describes its one HARD check in prose and is folded into the general `install.yml` smoke job alongside the other install-time checks, since it's a smoke-test invariant on the digest file rather than a PR-shaped lint. Treat the numbered HARD/WARN header and the dedicated workflow as `lint-pr-body.js`'s convention, not a requirement for every guard under `.github/scripts/`. Both still ship a same-directory `run-<name>-fixtures.js` (`run-lint-pr-body-fixtures.js`, `run-check-digest-preview-fixtures.js`), the same fixture-runner-next-to-the-thing-it-tests convention already recorded for `hooks/<harness>/scripts/run-<name>-*.js`. `check-digest-preview.js` also backs a documented invariant that has no other mechanical enforcement: `rules/core-rules.md` is a deliberate subset of `rules/agent-guidelines.md`, not a mirror (selection criteria in AGENTS.md's rule-delivery section), and the digest's first ~1,998 bytes must carry its three highest-value, least-mechanically-backed bullets in a fixed order, since Claude Code only shows the model that much of a large hook's stdout inline. Don't reorder or trim those three bullets without re-running this guard. [`check-split-shell-segments-parity.js`](.github/scripts/check-split-shell-segments-parity.js) is a third instance of the same category, backing a different documented-but-otherwise-unenforced invariant: that `splitShellSegments` stays byte-for-byte identical across its three standalone copies (`hooks/claude/plan-gate.js`, `hooks/codex/plan-gate-pilot.js`, `hooks/git-guard.js`), per the intentional-duplication DONT below. It extracts each copy's function body by exact start/end-line match rather than diffing whole files, throwing loudly (not passing silently) if a copy renames or restructures the function out from under the extractor, and reports the first line at which any pair diverges. Same same-directory fixture-runner convention as its two siblings (`run-check-split-shell-segments-parity-fixtures.js`).
- [`docs/hooks.md`](docs/hooks.md) (sibling: [`docs/installers.md`](docs/installers.md); README.md and `docs/models.md` follow the same shape at their own scale): the canonical reference-doc shape, replacing the dense single-paragraph-per-item shape this file previously documented as canonical for these same files (resolved through the one-question protocol; see the Style Evolution Log entry below). One H2 per component or topic (`## core-rules-digest.js`, `## hooks/claude/plan-gate.js`), broken into short paragraphs rather than one run-on bullet; a component with several independent sub-behaviors (`plan-gate.js`'s stamp/scope/mutation/content-lint/migration-guard/attribution-guard gates) gets one H3 per sub-behavior instead of folding all of them into a single paragraph. A cross-cutting note that isn't tied to one file gets its own top-level section before the per-item sections (`## Shared script architecture`, `## Overview`). A closed mapping or env-var-driven variance (Codex's model-compatibility table, Copilot's tool-alias table) renders as a markdown table, one row per pairing, rather than a prose sentence per pairing. This repo's own `.ai-style-rules.md` is exempt: its own prose density (as seen throughout this Golden Files section) is a byproduct of the inherit-legacy-style skill's format, not a style target other docs should follow. [`docs/out-of-scope.md`](docs/out-of-scope.md) is the same one-H2-per-topic shape applied to a fourth kind of content: a decision not to build something, so a later session doesn't re-propose ground already covered. Its own opening section states what does and doesn't belong here versus a component's own header (a decision about how an existing thing behaves) or `tasks/lessons.md` (a correction about how to work, gitignored and session-local where this file is checked in). Each decision entry names when it was last re-reviewed and treats itself as reopenable rather than a permanent ban; where a decision turns on the repo owner's own judgment call rather than a technical argument, it quotes their exact words in a blockquote instead of paraphrasing, so a later session can tell an actual stated preference from an inferred one.

## Naming & State-Control Rules

- **Env var flags**: `SCREAMING_SNAKE_CASE`, prefixed by the owning script/skill (`GATEGUARD_DISABLED`, `DELIVERY_GATE_BLOCK`, `COMPACT_THRESHOLD`). Boolean flags read as the string `"1"` (never `true`/`false`), and always default to the non-blocking/safe behavior when unset.
- **JS variables/functions**: `camelCase`. Exceptions are wire-format fields mirrored verbatim from JSON payloads the script didn't design (`tool_name`, `tool_input`, `session_id`): keep those snake_case to match the payload, don't rename them on the way in.
- **Bash local variables/functions**: `snake_case` (`est_tokens`, `line_count`, `extract_field`). Top-level script constants (computed-once paths, thresholds, config arrays, e.g. `REPO_DIR`, `SCRIPT_DIR`, `CLAUDE_ONLY_SKILLS`) use `SCREAMING_SNAKE_CASE` like the env var flags above, even when not env-derived.
- **`require` ordering**: alphabetical, always Node core modules only; no external npm deps in any hook script. (No bash script here uses `source`, so there's no matching convention to state for it.)
- **Stdin handling**: every script that reads JSON from stdin uses the identical two-step pattern: a `readStdin()` helper that returns `''` on any read failure (its `catch {}` is intentionally bare, like most safe-default catches in these hooks; see DONTs), then `JSON.parse(readStdin() || '{}')` wrapped in its own try/catch that falls back to `{}` (or an immediate safe exit) rather than throwing.
- **Subagent tool-call detection**: a hook that must tell a main-thread tool call apart from one issued by a dispatched subagent checks the same four fields (`agent_id`, `agentId`, `parent_tool_use_id`, `parentToolUseId`; true if any is a non-empty string): `gateguard.js`'s `isSubagent()` and `hooks/claude/suggest-compact.js`'s inline check use the identical field list, copied per script rather than factored into a shared helper (see the duplicated-helpers DONT below). `agent_type` alone is deliberately excluded: it's also set for a whole session launched with `--agent`, which is main-thread, not delegated.
- **Multi-harness logic**: one script sniffs the input shape at runtime (`detectDialect`) and branches, rather than forking a copy per harness (Claude/Codex/Copilot). The three `hooks/<harness>/*.json` files exist only to *wire* the shared script into that harness's hook contract; they carry no logic of their own.
- **Multi-harness branching picks its mechanism by applicability** (refines the rule above): a hook whose payload shape actually differs across harnesses sniffs it at runtime (`detectDialect`, e.g. `gateguard.js`, which wires into Claude, Codex, and Copilot); a hook with no distinguishing input branches on a static flag baked into the JSON wiring at install time (`core-rules-digest.js`'s `--copilot` argv check, since its harness difference is which event fires, not what the payload looks like: Copilot has no `UserPromptSubmit` at all, so there is no dialect to distinguish, even though the Claude/Codex payload does carry the submitted `prompt` that the answer-shape nudge reads); a hook installed only where the payload is already identical needs no branching at all (`delivery-gate.js`, Claude/Codex only, both sharing the same Stop payload contract). All three are house style; choose by whether the hook has a payload to inspect and whether that payload actually varies across the harnesses it's installed for. Don't "fix" the digest hook to use `detectDialect` (it reads its payload, but no dialect separates the harnesses it runs on), and don't add dialect-detection to `delivery-gate.js` (its installed harnesses already agree on the shape).
- **Subagent `model`/`effort` pinning**: `agents/*.md` pin `model` and `effort` in lockstep, but the lockstep pairing is per-agent-role, not a single fixed table keyed only on model name: `fable`↔`high` for the three read-only advisors (architect-reviewer, security-auditor, fable-advisor), `opus`↔`xhigh` for planner (the one role now using the tier the prior round called "valid but currently unused"), `sonnet`↔`high` for research/diagnosis/execution (researcher, debugger, executor), `haiku`↔`medium` for mechanical no-judgment edits (mechanic). `fable` no longer implies `xhigh` by itself; planner is the only `xhigh` pin left, reflecting a measured retiering (planner alone stayed pinned to the deepest-reasoning tier; the three advisors moved down a rung) rather than a uniform read-only-implies-strongest-effort rule. State the rationale inline in `description`, don't leave it implicit.
- **Skill frontmatter `description`**: optimize for routing first. Front-load the user's intent, concrete trigger terms, and the nearest negative boundary, never just a summary of what the skill does. Aim for a few sentences or a short paragraph, usually about 500 decoded characters when that preserves every routing signal; shorter and longer descriptions are both valid, and fresh positive or near-miss routing evidence justifies exceeding the target when extra clauses matter. Preserve failure-scar clauses that prevent known misroutes. The 1,024-character format maximum is separate; the 500-character review target is informational, never a CI or schema cap. [`skills/skill-activation/`](skills/skill-activation/SKILL.md) lints a related trigger-word heuristic and reports this soft length target.
- **Cross-skill references**: `[[skill-name]]` wiki-link syntax inside skill bodies (see `context-budget`, `skill-activation`) to point at a related skill without duplicating its content.
- **Copilot review-instruction files**: live at `.github/instructions/<bucket>.instructions.md`, one bucket per path scope (a repo-wide `applyTo: "**"` bucket for voice/hygiene/scope rules, plus one bucket per distinct area evidenced by the sources, e.g. `scripts`, `docs`). Frontmatter always sets `excludeAgent: "cloud-agent"`.
- **Hook denial pattern**: the default is loop-free by marking a gated path checked the moment the hook fires, regardless of whether that firing blocks or only warns; a file can only ever be gated once per session either way. `gateguard.js` is the reference instance and is now warn-by-default on Claude/Codex, deny-by-default on Copilot (see its Golden File entry above), so "loop-free by marking" no longer implies "marks at deny time" the way it did before this round. A hook gating on an external unlock event outside its own control (`plan-gate.js`, unlocked only by a `plan-and-track` Skill invocation elsewhere in the session) may deny repeatedly by design, and states that explicitly in its own header rather than leaving it to be inferred. `hooks/git-guard.js` adds a third keying granularity on top of the same mark-at-deny-time, 2-second racing-loser-window machinery plan-gate's migration-state and attribution guards already use: rather than a gated file path, four of its five kinds (`reset-hard`, `clean-force`, `force-push`, `discard-worktree`) each deny once per destructive-command *kind* per session, reusing the existing shape rather than inventing new machinery for the new key; the fifth, `stage-env-file`, keys instead on the candidate path itself, since two different directories can share a filename and a confirmed retry for one must not disarm the guard for the other.
- **Vendored-skill attribution comment**: a skill adapted wholesale from an external repo (`frontend-design`, `webapp-testing`, both from `anthropics/skills`) carries the HTML comment described in the Golden File above immediately after frontmatter, plus a sibling `LICENSE.txt` with the upstream license text. `theme-factory` was a third instance of this category until this round retired it into `frontend-design`; see that Golden File entry for how a fold-in extends rather than drops the retired skill's attribution. A skill that's merely *inspired by* another repo's idea but rewritten (e.g. `humanizer`) instead gets a one-line "adapted from X" credit in `AGENTS.md`'s Skills section, no in-file comment or `LICENSE.txt`; the distinction is whether the file content is substantially the vendor's own text or a from-scratch rewrite. The same inspired-by/rewritten credit now also covers a standing rule, not just a skill: the action-first output rule folded into `rules/agent-guidelines.md`/`rules/core-rules.md` is credited with a one-line "adapted from X" sentence in `AGENTS.md`'s rule-mechanism section rather than a skill-anatomy in-file comment, since it was deliberately implemented as always-on behavior instead of the upstream's opt-in, explicitly-invoked skill form. The same wholesale-vendoring shape also covers an `agents/*.md` file, not just a `SKILL.md`: [`agents/security-auditor.md`](agents/security-auditor.md)'s threat-model mode carries the attribution HTML comment above its adapted section, with the upstream Apache-2.0 text moved to a sibling `agents/security-auditor.LICENSE.txt`, after `skills/security-threat-model/` was folded into it.
- **Reference material lives in `docs/`, not README or AGENTS.md**: exhaustive reference detail (the model-default table per harness, the full tiered-subagent roster with per-agent descriptions, per-hook behavior and wiring, installer idempotency rules) lives in `docs/*.md`, one topic per file, with the source file linking to it rather than inlining it. This is the "Be skimmable, not exhaustive" rule applied structurally, not just to prose density: when a README or `AGENTS.md` section grows into a reference table or a per-item enumeration, that's the signal to extract it to `docs/` (`docs/hooks.md`, `docs/installers.md` are AGENTS.md-sourced instances of the same extraction, alongside README's own `docs/models.md`/`docs/skills.md`).
- **`docs/*.md` and `README.md` structure**: short paragraphs under H2/H3 section headers, one section per component or sub-behavior, with a markdown table for any closed mapping or per-harness variance table; see the `docs/hooks.md` Golden File above for the full shape. This replaces the file's own dense single-paragraph-per-item convention as of the 2026-08-03 docs-readability round; a doc edit under `docs/` or `README.md` should extend the sectioned shape, not revert to run-on bullets.
- **Bash to PowerShell function mirroring**: `install.sh` and `install.ps1` mirror function-for-function (`copy_agents`/`Copy-Agents`, `frontmatter_field`/`Get-AgentFrontmatterField`, `toml_escape`/`ConvertTo-TomlEscaped`, `render_codex_agent`/`ConvertTo-CodexAgentToml`, `upsert_toml_default`/`Set-TomlDefault`, and now `copy_copilot_agents`/`Copy-CopilotAgents`, `render_copilot_agent`/`ConvertTo-CopilotAgentMd`, `copilot_tools`/`ConvertTo-CopilotTools`). The PowerShell name always uses an approved Verb-Noun cmdlet verb; when the bash verb already maps onto one (`copy` → `Copy-`), the mirror looks like a case-transform, but that's incidental, not the rule. An unapproved bash verb gets substituted for the nearest real cmdlet verb (`upsert_toml_default` becomes `Set-TomlDefault`, not `Upsert-TomlDefault`; `render_copilot_agent` becomes `ConvertTo-CopilotAgentMd`, same substitution already recorded for the Codex renderer); don't flag a mirror just because its casing differs from the bash name. `frontmatter_field`/`Get-AgentFrontmatterField` grew matching escape-decoding logic: single-quoted `''` is a plain global replace (`gsub`/`-replace`), safe because a doubled single-quote has no adjacency ambiguity to mis-handle, while double-quoted `\"`/`\\` decodes in one left-to-right pass rather than sequential global replaces, which would mis-handle a backslash immediately before a quote; both abort the install on an escape neither installer's decoder can render, rather than installing a silently corrupted rendered value into two harnesses; `install.ps1` factors its decode loop into a sibling `ConvertTo-DecodedDoubleQuoted` while `install.sh`'s lives inline in an awk function, and that's not a naming gap to close, since PowerShell and awk don't share a convention for when to extract a helper. The two do diverge on purpose in one spot, self-documented in both files' comments: `install.ps1`'s `ConvertTo-CopilotAgentMd` safely nests `Get-AgentFrontmatterField` inside the `ConvertTo-CopilotTools` call because PowerShell's `throw` is a terminating error that unwinds through the nesting either way, but `install.sh`'s `render_copilot_agent` must assign `frontmatter_field`'s result to its own local variable first, since bash's `set -e` only observes the outer command's exit status and would silently swallow an inner command substitution's abort. Don't reconcile these two shapes; the divergence is the correct mirror, not drift.

## DONTs

- **Don't factor the duplicated JS helpers (`readStdin`, `intEnv`) into a shared module.** Each hook script is copied standalone into a different harness's scripts directory at install time (`~/.claude/scripts/`, `~/.codex/scripts/`, `~/.copilot/scripts/`) with no shared `node_modules` or relative-import root; every script must stay self-contained and runnable in isolation. This is stated explicitly in each file's header comment; the duplication is intentional, not oversight. The same rule now also covers the mutation gate's quote-aware classifier pair, though the two functions are no longer one inseparable duplication with one file list: `splitShellSegments`, the tokenizer, is duplicated byte-for-byte across three files, `hooks/claude/plan-gate.js`, `hooks/codex/plan-gate-pilot.js`, and `hooks/git-guard.js` (whose own header states the byte-for-byte contract and that the copy must not be hand-edited), while `detectOutwardMutations`, the classifier built on top of it, is duplicated only across the two plan-gate files; `git-guard.js` defines its own `detectDestructiveGit` instead and only references `detectOutwardMutations()` in cross-reference comments. Same underlying no-shared-module-root-at-install-time reason as the helpers above, just split across a wider and a narrower file list. This repo no longer trusts the header comment alone to hold the `splitShellSegments` copy in sync: [`.github/scripts/check-split-shell-segments-parity.js`](.github/scripts/check-split-shell-segments-parity.js) extracts the function body from all three files and hard-fails CI on any divergence, naming the divergent line. This is the pair-a-mandate-with-a-mechanical-check principle (see AGENTS.md's Skills section) applied to a DONT rather than a positive convention: a duplication this repo deliberately keeps gets a parity check, not just a comment asking future editors to behave. The rule extends past hook scripts too: the bounded-live-run process-control helpers (`liveCaseTimeout`, `terminateChildTree`, `handleParentSignal`, the timeout/grace/force-kill staging in `runChildCase`, all gated by a `LIVE_CASE_TIMEOUT_MS` env var) are duplicated across [`skills/skill-activation/scripts/run-activation-cases.js`](skills/skill-activation/scripts/run-activation-cases.js), its sibling [`run-behavioral-smokes.js`](skills/skill-activation/scripts/run-behavioral-smokes.js), and [`skills/skill-comply/scripts/run-codex-cases.js`](skills/skill-comply/scripts/run-codex-cases.js), since a skill's own `scripts/` directory has no shared-module root across sibling skills either, same underlying reason as the hook-script case. The two `skill-activation` runners carry these helpers verbatim, but `run-codex-cases.js` is a parallel implementation of the same design rather than a byte-for-byte copy: it calls `fail()` where they call `die()`, `childProcess.spawn()` where they call a destructured `spawn()`, and caps captured output with its own `MAX_CAPTURE_BYTES` that the activation runners don't define. Don't flag those script-specific differences as drift to reconcile.
- **Don't let an error escape a hook script uncaught.** Every hook is wrapped in an outermost `try { main() } catch (err) { ...allow...; process.exit(0) }`. A hook that throws can fail a harness closed (Copilot) or otherwise wedge a session; always fail open, and always `process.exit(0)` explicitly rather than relying on the implicit exit code.
- **Don't add a `catch {}` with zero recovery action.** Every catch either assigns/returns a self-evidently safe default (`''`, `null`, `{}`, `[]`; needs no comment, e.g. `readStdin()`'s `catch { return ''; }`) or, when the fallback's safety isn't obvious from the code alone (it changes control flow, retries, or picks a non-trivial value), carries a one-line `/* comment explaining why */` or a `process.stderr.write('[Tag] ...')` diagnostic. An empty catch that does neither, silently discarding the error with no recovery at all, is not the house style.
- **Don't add a top-level `scripts/` directory.** Skill-owned scripts live at `skills/<name>/scripts/`, next to the `SKILL.md` that documents them; hook scripts live at `hooks/` (shared across harnesses) or `hooks/<harness>/` (that harness's own implementation, e.g. `hooks/claude/suggest-compact.js`, plus the JSON files that wire a shared script into that harness's hook contract). A hook's fixture-driven regression tests live one directory level below the hook script itself: `hooks/<harness>/scripts/run-<name>-*.js` plus `hooks/<harness>/fixtures/<name>/cases.json` for a harness-specific hook (e.g. `hooks/codex/scripts/run-plan-gate-pilot-fixtures.js` next to `hooks/codex/plan-gate-pilot.js`), or `hooks/scripts/run-<name>-*.js` plus `hooks/fixtures/<name>/cases.json` for a hook shared across all harnesses and installed from `hooks/` itself (e.g. `hooks/scripts/run-git-guard-fixtures.js` next to `hooks/git-guard.js`). Nothing lives in a centralized, ungrouped scripts folder: `hooks/scripts/` is still grouped one level below the script it tests, exactly as `hooks/codex/scripts/` is.
- **Don't hand-edit an installed copy.** `rules/agent-guidelines.md` and `rules/core-rules.md` are the only source of truth for rule text; every per-harness copy (`~/.claude/CLAUDE.md` managed block, `~/.claude/core-rules.md`, etc.) is generated by `install.sh` and is expected to be overwritten on the next install. Edit the repo copy and re-run `./install.sh all`.
- **Don't normalize the Copilot JSON hook shape to match Claude/Codex's**, or vice versa (see Golden Files above); the differing keys are a wire-contract requirement, not inconsistency to fix.
- **Don't skip the dependency gate in a new bash script.** When `jq` is required for the script to do its job (`install.sh`'s `need_jq()`), check `command -v jq` first; don't let a missing dependency surface as a cryptic downstream failure instead. A gate is still allowed to offer a fix instead of only erroring (`need_jq()` now offers to auto-install `jq` via the platform's package manager), but auto-remediating a missing dependency is never silent: it needs interactive confirmation or an explicit non-interactive opt-in (`PT_INSTALL_JQ=1`, following the env-var-flag naming convention below), and every unresolved path still exits 1 with an `error: ...` message on stderr, exactly like the old gate. When one step is genuinely optional, like `install.sh`'s Copilot model default, warn and skip that step rather than exiting.
- **Don't silently regenerate or hand-edit a `.github/instructions/*.instructions.md` file without first checking for the `<!-- Generated by copilot-review-instructions; ... -->` marker comment.** Present → the file is skill-owned; safe to fully regenerate. Absent → don't assume hand-authored from that alone; ask the user to confirm actual provenance (the three original files turned out to be AI-generated in an earlier session, just predating the marker convention) before deciding whether to overwrite.
- **Don't treat the checked-in `hooks/<harness>/*.json` as the final hook artifact.** They are pre-substitution templates carrying a literal `__SCRIPTS__` placeholder; `install.sh`'s `render_hook` (and `install.ps1`'s mirror) bakes in the resolved absolute scripts dir (forward-slashed, even on Windows) at install time. Edit the placeholder form in the template, never a rendered absolute path; the rendered file lives in `~/.claude/settings.json` etc. and is overwritten each install (see the installed-copy DONT above).
- **Don't let `.ps1` be normalized to LF, or any `.sh`/`.js`/`.json`/`.md` to CRLF.** `.gitattributes` forces LF on shell/Node/JSON/markdown (a CRLF shebang breaks bash; hook scripts and JSON are read on every platform) and CRLF on `.ps1` only (the Windows-native installer). PowerShell is the one intentional CRLF exception; don't "normalize" it away.
- **Don't vendor a skill whose upstream license forbids redistribution.** The `docx`/`pdf`/`pptx`/`xlsx` skills carry a license that only permits use within Anthropic's own Services, so they're never copied under `skills/`; `install-office-skills.sh`/`.ps1` instead fetches them live from `anthropics/skills` at install time via a pinned third-party CLI. A skill under a permissive license (Apache-2.0: `frontend-design`, `webapp-testing`) can be vendored, but must carry the attribution comment and `LICENSE.txt` from the Naming rule above; check the upstream license before choosing vendor-in-repo vs. fetch-at-install for a new adapted skill.
- **Don't change the current Codex compatibility mapping or apply it to Copilot.** `render_codex_agent`/`ConvertTo-CodexAgentToml` renders `fable`/`opus` as `gpt-5.6-sol`, `sonnet` as `gpt-5.6-terra`, and `haiku` as `gpt-5.6-luna`, preserving `effort` as `model_reasoning_effort` and `tools` as `sandbox_mode` (Edit/Write present ⇒ `workspace-write`, else `read-only`). `render_copilot_agent`/`ConvertTo-CopilotAgentMd` still leaves `model` unset and drops `effort`, since Copilot's frontmatter has no matching fields; only its closed `tools` alias table translates. Because rendered copies vary in whether `model`/`effort` appear, an `agents/*.md` `description` should name the tier generically ("the roster's strongest-judgment tier", "a cheap execution tier") rather than a specific model name, so the prose stays accurate across all three harnesses; frontmatter remains the source of truth.
- **Don't interpolate attacker-controlled text straight into a GitHub Actions `run:` block.** [`.github/workflows/pr-body-lint.yml`](.github/workflows/pr-body-lint.yml) writes `${{ github.event.pull_request.body }}` to `$RUNNER_TEMP` through an `env:` var (`PR_BODY`) and a `printf '%s'`, never through direct expression interpolation inside `run:`, because a PR body is attacker-controlled on a fork PR and a `${{ }}` expression interpolated into a shell script is template substitution before the shell ever runs, not a shell-quoted value. The lint script itself then reads that file rather than an argument or env var holding the raw body. Apply the same env-var-then-file shape to any future workflow step that has to touch PR/issue title, body, or comment text.
- **Don't use `*` as the markdown bullet marker in this repo's own doc prose.** `docs/*.md`, `README.md`, and `AGENTS.md` bullets are otherwise uniformly `-`; `*` is expected inside a fenced block quoting external template text (e.g. `docs/tag-and-release.md`'s release-notes template). One known exception sits outside a fence: `docs/tag-and-release.md`'s own six-item pre-publish verification checklist predates this rule and hasn't been cleaned up. Flag any other `*`-marked bullet outside a fenced block as a new instance of this drift; don't re-flag that known one on every pass.
- **Don't leave a colon-space inside an unquoted YAML frontmatter value.** A SKILL.md `description:` (or any frontmatter value) containing `: ` must be quoted: double quotes by default, single quotes when the text already contains a double quote, and when it contains both quote types, escape the chosen delimiter (`\"` inside double quotes, `''` inside single quotes) rather than switching representations; an unquoted colon-space reads as a nested YAML mapping key and invalidates the whole frontmatter block for a strict parser, even though this repo's own lenient line-regex parsers kept working, which is why the failure shipped silently across five skills before being caught. [`skills/skill-activation/scripts/run-activation-cases.js`](skills/skill-activation/scripts/run-activation-cases.js)'s `--precheck` `frontmatter_invalid_yaml` flag (and [`skills/rules-distill/scripts/scan-skills.js`](skills/rules-distill/scripts/scan-skills.js)'s matching quote-stripping) now lints for this and its mirror case, a quoted value containing an unescaped copy of its own delimiter. The same requirement covers `agents/*.md` `description:` fields too, not just SKILL.md: `architect-reviewer.md`, `fable-advisor.md`, `planner.md`, and `security-auditor.md` all quote their description now that it contains a `Read-only:`-style colon-space, while the unaffected agent files (`debugger.md`, `executor.md`, `mechanic.md`, `researcher.md`) stay unquoted; `run-activation-cases.js`'s new `--precheck-agents` mode lints this for agents the same way `--precheck` does for skills. `frontmatter_invalid_yaml`'s plain-scalar check has since widened past colon-space alone to the full YAML 1.2.2 plain-scalar grammar (cited by spec section/production number in the script's own comment): a leading indicator character, a leading `?`/`:`/`-` not followed by a non-space, a `:` followed by space/tab/end-of-value anywhere in the value, or a `#` preceded by space/tab anywhere in the value all now flag, not just the original `: ` case. A sibling `--precheck-agents`-only flag, `frontmatter_uninstallable_escape`, catches a narrower and different problem: a double-quoted value that is legal YAML (e.g. `\t`, `\n`, `\uXXXX`) but uses an escape neither installer's decoder knows how to render, since that decoder only implements `\"` and `\\`. A value can trip one flag, the other, both, or neither; don't collapse them into one check or assume `frontmatter_invalid_yaml === false` means an agent file is safe to install.

## Style Evolution Log

Compacted 2026-08-24 at `9c5828d` (HEAD): every live convention that lived
only in an older entry was promoted into the canonical sections above (see
the Vendored-skill attribution comment rule), superseded and reverted
entries were dropped, and only the three newest entries below were kept for
recency. Older entries live in git history, not in this file.

### [2026-08-25]: no new source since last fingerprint

Diffed `9c5828d` → `32d2933` (HEAD; 1 commit since the last-recorded fingerprint, and that one commit, `32d2933`, is this file's own prior-round compaction commit that produced the fingerprint just diffed from; not new source to analyze).

No new source commits to review this round. No genuine conflicts across the four scan dimensions; nothing deferred.

### [2026-08-24]: stage-env-file git-guard tripwire and the never-commit-secrets rule

Diffed `0f5c239` → `9c5828d` (HEAD; 2 commits since the last-recorded fingerprint, one of which, `5660b66`, is this file's own prior-round analysis commit that produced the fingerprint just diffed from; not new source to analyze).

- `9c5828d` (#216, "Guard against committing .env files, and document the CI-guard commands"): adds a never-commit-secrets clause to both rule files, a fifth `stage-env-file` kind to `hooks/git-guard.js`, and two AGENTS.md Commands lines naming the free digest-preview and splitShellSegments-parity guards. All clean instances of recorded conventions: the new kind reuses the existing `claimMarker` deny-once machinery with a new path-keyed granularity (already folded into the hook-denial Naming rule by that PR's own correction of this file), its pathspec reading is a closed, header-documented classifier whose accepted gaps are stated as decisions in the header and `docs/hooks.md` rather than left as untested corners, and the extended fixture suite follows the fixture-runner-next-to-hook shape. Nothing promoted to a new rule.

No genuine conflicts across the four scan dimensions; nothing deferred.

### [2026-08-23]: Concise output-style managed default

Diffed `bd973b7` → `0f5c239` (HEAD; 2 commits since the last-recorded fingerprint, one of which, `ed5ac30`, is this file's own prior-round analysis commit that produced the fingerprint just diffed from; not new source to analyze).

- `0f5c239` (#214, "Install Concise as the managed Claude output-style default"): adds `outputStyle: Concise` to install.sh/install.ps1's Claude managed defaults via the same `set_json_default`/`Set-JsonDefault` helper already used for `model`/`switchModelsOnFlag`, using the same "Repo-owned X, re-asserted on every install: ..." rationale-comment template already established immediately below it in both installers (the plugin-disable comment). `PT_KEEP_MODEL`'s documented scope widens from "model settings only" to "model and output-style settings only" everywhere it was stated, a factual-scope correction in the same vein as prior rounds' comment corrections, not a new convention. `docs/installers.md` and `docs/models.md` rename their "Model defaults" heading and lead sentences to "Managed defaults" to match, and `docs/models.md`'s table gains one row following its own already-recorded closed-mapping-as-table convention. `.github/workflows/install.yml` and its PowerShell mirror gain a new smoke-test step following the existing scratch-`$HOME` CI shape, paired with a negative control on the same fixture proving a bare install overwrites it, the identical not-vacuous-assertion pattern already used for `assert_bare_permission_posture`'s bypass-key check. All clean instances of already-recorded conventions; nothing promoted to a new rule.

No other genuine conflicts across the four scan dimensions.

### [2026-08-17]: knowledge-graph memory and ruflo declined, skill-comply's evidence non-overlap rule

Diffed `826b6c9` → `bd973b7` (HEAD; 2 commits since the last-recorded fingerprint, one of which, `5bb3d7f`, is this file's own prior-round analysis commit that produced the fingerprint just diffed from; not new source to analyze).

- `bd973b7` (#212, "Decline knowledge-graph memory and ruflo, and require non-overlapping evidence in skill-comply"): adds two new `docs/out-of-scope.md` entries declining a proposed knowledge-graph-memory skill/fold-in and declining adoption of `ruvnet/ruflo`. Both are clean instances of the canonical `docs/out-of-scope.md` shape already recorded (a dated "Proposed on ..." opening, a reopenable close naming two things that would revisit the decision); the ruflo entry paraphrases rather than quotes the owner's ask, since the decision turns on a technical evaluation rather than a stated preference, consistent with the existing blockquote-vs-paraphrase rule. It also adds a new rule to `skills/skill-comply/SKILL.md` (execution evidence and verification evidence in a trace must never be the same tool event) and credits it to HKUDS/OpenSpace in `AGENTS.md`'s Skills paragraph, a clean instance of the already-recorded inspired-by/rewritten credit convention.
  - Deferred conflict: that same `skill-comply/SKILL.md` addition also cites HKUDS/OpenSpace inline, in a parenthetical inside the rule's own body text ("(Adapted from HKUDS/OpenSpace's capture contract, ...)"), rather than confining the credit to `AGENTS.md` alone. The one existing precedent for a *secondary* source crediting a single section within an already-differently-attributed skill, `migration-discipline`'s three sections adapted from `mattpocock/skills` per `AGENTS.md`, carries no matching inline citation anywhere in that skill's body. Whether skill-comply's inline parenthetical is a deliberate new exception worth a repeatable per-section citation convention, or a slip that should be trimmed to match migration-discipline's AGENTS.md-only precedent, is a genuine fork on a documented rule; left unresolved rather than guessed at.

No other genuine conflicts across the four scan dimensions.

### [2026-08-15]: answer-shape nudge added to core-rules-digest.js, tag-and-release title requirement

Diffed `b8816bc` → `826b6c9` (HEAD; 3 commits since the last-recorded fingerprint, one of which, `c97d345`, is this file's own prior-round analysis commit that produced the fingerprint just diffed from; not new source to analyze).

- `9e26d8e` (#209, "Add an answer-shape rule and back it at prompt submission"): adds the "answer the question asked" standing rule to `rules/agent-guidelines.md` and `rules/core-rules.md`, and backs it mechanically by having `hooks/core-rules-digest.js` read the `UserPromptSubmit` payload's `prompt` and append a one-line nudge when it's question-shaped (a trailing `?` outside fenced blocks and inline code spans). The PR's own commits already updated this file's Naming rule and Style Evolution Log entry for the pre-existing `--copilot` argv-branch rationale (a stale-claim fix, not new source), and `.github/scripts/check-digest-preview.js` grew a matching reserve so the size guard checks what the hook actually prints (digest plus nudge) against the inline-persistence threshold, not the digest file alone; both are clean instances of the already-recorded pair-a-mandate-with-a-mechanical-check principle and the lockstep-with-`docs/hooks.md` pattern for a hook gaining a new check. The new fixture pair (`hooks/fixtures/core-rules-digest/cases.json` + `hooks/scripts/run-core-rules-digest-fixtures.js`) is a clean instance of the fixture-runner-next-to-hook shape, wired into `install.yml`'s Unix and Windows smoke jobs the same way its siblings are.
  - Deferred conflict: `core-rules-digest.js`'s new `readStdin()` adds a `process.stdin.isTTY` guard (skip reading rather than block waiting for EOF when run by hand) that no other copy in the Stdin handling rule's "byte-for-byte identical" set (`gateguard.js`, `delivery-gate.js`, `git-guard.js`, `hooks/claude/suggest-compact.js`, `hooks/claude/plan-gate.js`, `hooks/codex/plan-gate-pilot.js`) carries. Whether this is a deliberate one-off (this hook is the one most likely to be run by hand while testing the nudge) or a latent hang-on-manual-run bug the other five should also get is a genuine fork on a rule this repo enforces by CI parity check elsewhere (`check-split-shell-segments-parity.js`); left unresolved rather than guessed at, so the Stdin handling rule's "identical" wording is unchanged and no new copy of the TTY guard was added or removed anywhere.
- `826b6c9` (#210, "Require a release title in the tag-and-release process"): `docs/tag-and-release.md`'s publish step now requires `gh release create ... --title <new-tag>`, with the rationale and the tag-race warning split into their own short paragraphs. This is a clean instance of the already-recorded canonical `docs/*.md` shape (short paragraph per topic); no new Golden File or rule needed.

No genuine conflicts across the four scan dimensions beyond the one deferred above.
