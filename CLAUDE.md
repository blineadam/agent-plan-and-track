# Agent Helpers

## Style conventions

This repo's implicit conventions are captured in `.ai-style-rules.md` at the project root. Read it before writing or editing code here, and follow its Golden Files, naming/state-control rules, and DONTs.

## Local additions

If this checkout has a `CLAUDE.local.md` next to this file, read it too: it holds machine-local tooling instructions (e.g. graphify) that aren't checked in because not every checkout has that tooling installed.
