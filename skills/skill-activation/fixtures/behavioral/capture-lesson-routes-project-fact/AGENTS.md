# AGENTS.md

This is a tiny Node.js service that exposes a handful of HTTP endpoints for
managing a to-do list. The project has no build step; source lives directly
under `src/` and is run with plain Node.

## Commands

- `npm test`: runs the suite.
- `npm run lint`: runs the linter.
