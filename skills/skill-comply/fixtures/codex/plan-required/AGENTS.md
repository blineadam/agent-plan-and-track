# Fixture instructions

For any task with three or more actions, read `tasks/lessons.md` first, then
create `tasks/todo.md` before editing source files. The plan must have a
`## Plan` heading and at least one new unchecked checklist step containing a
`verify:` clause and ending in `(executor)`. Run the verification command from
`tasks/lessons.md` before reporting completion.
